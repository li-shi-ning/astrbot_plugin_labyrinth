# Changelog

## v0.3.2

- 变更：棋盘右侧改为**独立的手牌卡区域**——把手牌放大到 120px 展示，
  并显示朝向、开口方向、推牌提示（上下选列 b/d/f、左右选行 2/4/6）与玩家进度。
- 新增：`test_board_has_hand_panel` 保证 4 人局面板内容不溢出。


## v0.3.1

- 变更：移动阶段改为**一个「移动」按钮**（点完补成 `迷宫 走 `，坐标自己填），
  不再生成 12 个格子按钮；文字里提示 `迷宫 走 c3` 的写法并列出可达格子。
- 说明：可达性仍由引擎校验，走不到的坐标会被拒绝。


## v0.3.0

- 变更：坐标体系改为 **列 a-g（左→右）+ 行 1-7（下→上）**，移动用 `迷宫 走 c3`。
- 变更：推牌改为 **上下推选列字母（b/d/f）、左右推选行号（2/4/6）**，
  按钮排成 **3 行 × 4 列、一列一边**（推上b/推下b/推左2/推右2 …）。
- 新增：移动阶段给出 **可达格子按钮**（最多 12 个「走xx」，按坐标排序不泄露目标）。
- 变更：对局结束**播报获胜玩家（带字母+昵称）并自动销毁房间**，最后一张棋盘仍会发出。
- 新增：**管理员**也可以用 `迷宫 解散` 销毁房间（对局中同样可用），
  未开局与进行中的键盘都带「解散牌局」按钮。
- 修复：未开局时回合提示误显示「轮到你推牌」。


## v0.2.1

- 变更：宝藏提示改为「emoji + 中文名」（如 `🎯 A 目标：🐉 龙`），
  私密目标按钮、找到提示、公开已收集列表、规则文档全部统一。
- 变更：私密目标按钮标签加 🎯（`🎯 A·目标`）。


## v0.2.0

- 新增：接入 SVG 素材包（`scripts/build_assets.js` 用 resvg 栅格化），内置
  3 张牌型底图 + 24 个宝藏图标 + 4 个棋子到 `assets/`。
- 变更：宝藏 id / 名称与素材包对齐（骑士头盔、烛台、剑、宝石、宝箱、戒指、藏宝图、
  钥匙、王冠、瓶中幽灵、金币袋、书 / 老鼠、蝙蝠、猫头鹰、蜥蜴、蜘蛛、飞蛾、
  圣甲虫、骷髅、龙、公主、女巫、幽灵）。
- 变更：牌型底图自动统一到本插件 `rotation=0` 朝向（素材包 I 型是横向、T 型缺左侧）。
- 变更：宝藏图标叠加尺寸调整为半格。


## v0.1.0

- 首个版本：QQ 官方群聊《Labyrinth 疯狂迷宫》。
- 引擎：7×7 棋盘（16 固定牌 + 33 可移动牌 + 1 手牌）、推牌带动棋子、
  被挤出棋盘的棋子回到插入格、可达性 BFS、宝藏收集与回起点判胜。
- 交互：推牌 / 旋转手牌 / 移动 / 停手，私密目标按钮，本地渲染棋盘图片走富媒体发送。
